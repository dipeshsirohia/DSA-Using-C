# include <stdio.h>
int main(){
    char infix[]={'a','+','b','*','c'};
    for(int i=0;i<=5;i++){
        if(infix[i]=='a' && 'b' && 'c'){

        }
        else{
            if(infix[i]=='+'||'*'){
                continue;
            }
        }

    }
}
